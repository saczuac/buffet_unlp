<table summary="Tabla de usuarios del buffet">
	<tr>
		<th id="Nombre">NOMBRE</th>
		<th id="MiembroDesde">DOCUMENTO</th>
		<th>ACCIONES</th>
	</tr>
		{% for usuario in usuarios %}
		  <tr id="{{ usuario.id }}">
				<td>{{ usuario.usuario }}</td>
				<td>{{ usuario.documento }}</td>
				<td>
					<a href="usuarios/edit/{{ usuario.id }}" title="Editar"><i class="fa fa-pencil"></i></a>
					<a href="usuarios/delete/{{ usuario.id }}" title="Eliminar"><i class="fa fa-trash-o"></i></a>
				</td>
			</tr>
		{% endfor %}
	</table>
