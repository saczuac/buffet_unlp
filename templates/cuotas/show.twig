{% extends "layout.twig" %}

{% block contenido %}

<div class="container">
	<div class="col col-6 card">
		<h2 class="title">
			<span class="icon">
				<i class="fa fa-user"></i>
			</span>
			{{ cuota.anio }} {{ cuota.mes }}
		</h2>
		<div class="student-data">
			<p><span class="field">Numero: </span>{{ cuota.numero }}</p>
			<p><span class="field">Monto: </span>{{ cuota.monto }}</p>
			<p><span class="field">Tipo: </span>{{ cuota.tipo}}</p>
			<p><span class="field">comision del cobrador: </span>{{ cuota.comisionCobrador}}</p>
			<p><span class="field">Fecha de alta: </span>{{ cuota.fechaAlta}}</p>
		</div>
		<div class="right">
			<a href="#"><button class="btn"><i class="fa fa-pencil"></i>Modificar</button></a>
			<a href="#"><button class="btn btn-danger"><i class="fa fa-trash-o fa"></i>Eliminar</button></a>
		</div>
	</div>

	
</div>

{% endblock %}